name: bmicalculator
description: bmi gemok kurus

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 2.0.4+11

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_svg: ^2.0.7
  fluttertoast: ^9.0.0
  font_awesome_flutter: ^11.0.0
  google_fonts: ^8.0.2
  package_info_plus: ^9.0.0
  provider: ^6.0.3
  share_plus: ^12.0.1
  syncfusion_flutter_gauges: ^32.2.9
  url_launcher: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
